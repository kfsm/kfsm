{erl_opts, [
]}.

{lib_dirs, ["deps"]}.
{cover_enabled, true}.

{deps, [
   % fogfish erlang toolkit archive
   {feta, ".*",
      {git, "https://github.com/fogfish/feta", {branch, master}}
   }
]}.

